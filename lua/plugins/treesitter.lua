return {
    "nvim-treesitter/nvim-treesitter",
    lazy = false,
    build = ":TSUpdate",
    -- config = function()
    -- end
    -- event = "VeryLazy",
    -- opts = {
    --     ensure_installed = { "lua" },
    --     highlight = { enable = true }
    -- },
}
